local ReplicatedFirst = game:GetService("ReplicatedFirst")
local Fusion = require(ReplicatedFirst.Packages.fusion)

local New = Fusion.New

return function(loadingBar)
    return New "ScreenGui" {

    }
end